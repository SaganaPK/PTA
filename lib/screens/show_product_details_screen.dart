import 'package:flutter/material.dart';

class show_product_details_screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Show Product Details'),
      ),
      body: Center(
        child: Text('This is the Show Product Details Screen'),
      ),
    );
  }
}